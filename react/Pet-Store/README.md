# 🐾 متجر الحيوانات الأليفة - Pet Store

مشروع متكامل لتجارة إلكترونية متخصصة في بيع منتجات الحيوانات الأليفة (طعام، ألعاب، إكسسوارات) مبني باستخدام React و Node.js.

## 📋 جدول المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المميزات](#المميزات)
- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والتشغيل](#التثبيت-والتشغيل)
- [الصفحات والوظائف](#الصفحات-والوظائف)
- [API Endpoints](#api-endpoints)
- [المتطلبات](#المتطلبات)
- [المساهمة](#المساهمة)

## 🎯 نظرة عامة

متجر الحيوانات الأليفة هو تطبيق ويب متكامل يوفر تجربة تسوق سلسة لعشاق الحيوانات الأليفة. يتضمن النظام إدارة كاملة للمنتجات، الطلبات، المستخدمين، والمدفوعات عبر PayPal.

## ✨ المميزات

### للمستخدمين العاديين:
- 🏠 **صفحة رئيسية جذابة** مع أقسام متعددة:
  - Hero Section مع صور تفاعلية
  - قسم About مع معلومات المتجر
  - عرض أفضل المنتجات من كل فئة
  - قسم العلامات التجارية مع سلايدر متحرك
  - عداد تنازلي للعروض الخاصة
  - قسم الشهادات (Testimonials)
- 🛍️ **تصفح المنتجات** مع فلاتر متقدمة:
  - فلترة حسب الفئة
  - فلترة حسب العلامة التجارية
  - فلترة حسب السعر
  - بحث وتصفح سهل
- ❤️ **قائمة المفضلة** لحفظ المنتجات المفضلة
- 🛒 **سلة التسوق** مع إدارة الكميات
- 💳 **الدفع الآمن** عبر PayPal
- 📦 **تتبع الطلبات** وعرض تاريخ الطلبات
- 👤 **إدارة الملف الشخصي** وتحديث البيانات

### للمديرين:
- 📊 **لوحة تحكم إدارية** شاملة مع:
  - إحصائيات المبيعات
  - عدد العملاء
  - عدد الطلبات
  - رسوم بيانية للمبيعات
- 📦 **إدارة المنتجات**:
  - إضافة منتجات جديدة
  - تعديل المنتجات
  - عرض جميع المنتجات مع pagination
  - رفع الصور
- 🏷️ **إدارة الفئات** (Categories)
- 👥 **إدارة المستخدمين**
- 📋 **إدارة الطلبات** مع تحديث حالة التسليم
- 📈 **تقارير وإحصائيات** مفصلة

### مميزات تقنية:
- 📱 **تصميم متجاوب بالكامل** يعمل على جميع الأجهزة
- ⚡ **أداء عالي** مع React و Vite
- 🔐 **أمان متقدم** مع JWT Authentication
- 🎨 **واجهة مستخدم عصرية** مع Tailwind CSS
- 🔄 **إدارة الحالة** مع Redux Toolkit
- 📡 **API متكامل** مع RTK Query

## 🛠️ التقنيات المستخدمة

### Frontend:
- **React 19.2.0** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **Redux Toolkit** - إدارة الحالة
- **React Router DOM** - التوجيه
- **Tailwind CSS** - التصميم
- **React Icons** - الأيقونات
- **React Slick** - السلايدرات
- **ApexCharts** - الرسوم البيانية
- **PayPal SDK** - الدفع الإلكتروني
- **Axios** - طلبات HTTP
- **React Toastify** - الإشعارات

### Backend:
- **Node.js** - بيئة التشغيل
- **Express.js** - إطار العمل
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ODM لـ MongoDB
- **JWT** - المصادقة
- **Bcryptjs** - تشفير كلمات المرور
- **Multer** - رفع الملفات
- **Cookie Parser** - معالجة الكوكيز

## 📁 هيكل المشروع

```
Pet-Store/
├── back-end/                 # الخادم الخلفي
│   ├── config/
│   │   └── db.js            # إعدادات قاعدة البيانات
│   ├── controllers/         # معالجات الطلبات
│   │   ├── userController.js
│   │   ├── productController.js
│   │   ├── categoryController.js
│   │   ├── orderController.js
│   │   └── categoryCountController.js
│   ├── middlewares/         # Middleware
│   │   ├── authMiddleware.js
│   │   ├── asyncHandler.js
│   │   └── checkId.js
│   ├── models/              # نماذج قاعدة البيانات
│   │   ├── userModel.js
│   │   ├── productModal.js
│   │   ├── categoryModel.js
│   │   └── orderModel.js
│   ├── routes/              # مسارات API
│   │   ├── userRoutes.js
│   │   ├── productRoutes.js
│   │   ├── categoryRoutes.js
│   │   ├── orderRoutes.js
│   │   └── uploadRoutes.js
│   ├── utils/
│   │   └── createToken.js
│   └── index.js             # نقطة البداية
│
├── front-end/               # الواجهة الأمامية
│   ├── src/
│   │   ├── components/     # المكونات المشتركة
│   │   │   ├── Header.jsx
│   │   │   ├── Footer.jsx
│   │   │   ├── HeroSection.jsx
│   │   │   ├── AboutSection.jsx
│   │   │   ├── BrandSection.jsx
│   │   │   ├── Count.jsx
│   │   │   ├── Testimonials.jsx
│   │   │   └── ...
│   │   ├── pages/          # الصفحات
│   │   │   ├── Home.jsx
│   │   │   ├── Shop.jsx
│   │   │   ├── Cart.jsx
│   │   │   ├── Auth/
│   │   │   ├── Admin/
│   │   │   ├── User/
│   │   │   ├── Orders/
│   │   │   └── products/
│   │   ├── redux/          # إدارة الحالة
│   │   │   ├── store.js
│   │   │   ├── api/
│   │   │   └── features/
│   │   ├── assets/         # الصور والموارد
│   │   └── utils/          # أدوات مساعدة
│   ├── package.json
│   └── vite.config.js
│
├── uploads/                 # الصور المرفوعة
├── Pet-Images/              # صور المنتجات
└── package.json             # ملف الجذر
```

## 🚀 التثبيت والتشغيل

### المتطلبات الأساسية:
- Node.js (v18 أو أحدث)
- MongoDB (محلي أو Atlas)
- npm أو yarn

### خطوات التثبيت:

1. **استنساخ المستودع:**
```bash
git clone <repository-url>
cd Pet-Store
```

2. **تثبيت التبعيات:**
```bash
# تثبيت تبعيات الجذر
npm install

# تثبيت تبعيات الواجهة الأمامية
cd front-end
npm install
cd ..

# تثبيت تبعيات الخادم الخلفي
cd back-end
npm install
cd ..
```

3. **إعداد متغيرات البيئة:**

أنشئ ملف `.env` في مجلد `back-end/`:

```env
PORT=5000
MONGO_URI=mongodb://localhost:27017/pet-store
JWT_SECRET=your_jwt_secret_key
PAYPAL_CLIENT_ID=your_paypal_client_id
```

4. **تشغيل المشروع:**

```bash
# تشغيل الخادم الخلفي والواجهة الأمامية معاً
npm run dev

# أو تشغيل كل واحد على حدة:
npm run back-end    # الخادم الخلفي على المنفذ 5000
npm run front-end   # الواجهة الأمامية على المنفذ 5173
```

5. **الوصول للتطبيق:**
- الواجهة الأمامية: `http://localhost:5173`
- API: `http://localhost:5000`

## 📄 الصفحات والوظائف

### صفحات عامة:
- **الصفحة الرئيسية** (`/`): عرض المنتجات الخاصة، أفضل المنتجات، العلامات التجارية
- **المتجر** (`/shop`): تصفح جميع المنتجات مع فلاتر
- **تفاصيل المنتج** (`/product/:id`): عرض تفاصيل المنتج، التقييمات، إضافة للعربة
- **سلة التسوق** (`/cart`): عرض وإدارة المنتجات في السلة

### صفحات المستخدم:
- **تسجيل الدخول** (`/login`)
- **التسجيل** (`/register`)
- **الملف الشخصي** (`/profile`): تحديث البيانات
- **طلباتي** (`/user-orders`): عرض تاريخ الطلبات
- **المفضلة** (`/favorite`): المنتجات المفضلة

### صفحات الطلب:
- **الشحن** (`/shipping`): إدخال عنوان الشحن
- **تأكيد الطلب** (`/placeorder`): مراجعة الطلب والدفع
- **تفاصيل الطلب** (`/order/:id`): تفاصيل الطلب والدفع

### صفحات المدير:
- **لوحة التحكم** (`/admin/dashboard`): الإحصائيات والرسوم البيانية
- **إدارة المنتجات** (`/admin/productlist`): إضافة/تعديل المنتجات
- **جميع المنتجات** (`/admin/allproductslist`): عرض جميع المنتجات
- **إدارة الفئات** (`/admin/categorylist`)
- **إدارة المستخدمين** (`/admin/userlist`)
- **إدارة الطلبات** (`/admin/orderlist`)

## 🔌 API Endpoints

### المستخدمون:
- `POST /api/users/register` - تسجيل مستخدم جديد
- `POST /api/users/login` - تسجيل الدخول
- `GET /api/users/profile` - الحصول على الملف الشخصي
- `PUT /api/users/profile` - تحديث الملف الشخصي
- `GET /api/users` - الحصول على جميع المستخدمين (مدير)

### المنتجات:
- `GET /api/products` - الحصول على جميع المنتجات
- `GET /api/products/:id` - الحصول على منتج محدد
- `GET /api/products/special` - المنتجات الخاصة
- `POST /api/products` - إضافة منتج جديد (مدير)
- `PUT /api/products/:id` - تحديث منتج (مدير)
- `DELETE /api/products/:id` - حذف منتج (مدير)

### الفئات:
- `GET /api/category` - الحصول على جميع الفئات
- `GET /api/category/count` - أفضل منتج من كل فئة
- `POST /api/category` - إضافة فئة جديدة (مدير)
- `PUT /api/category/:id` - تحديث فئة (مدير)
- `DELETE /api/category/:id` - حذف فئة (مدير)

### الطلبات:
- `POST /api/orders` - إنشاء طلب جديد
- `GET /api/orders` - الحصول على جميع الطلبات (مدير)
- `GET /api/orders/myorders` - طلبات المستخدم
- `GET /api/orders/:id` - تفاصيل طلب محدد
- `PUT /api/orders/:id/pay` - تحديث حالة الدفع
- `PUT /api/orders/:id/deliver` - تحديث حالة التسليم (مدير)

### رفع الملفات:
- `POST /api/upload` - رفع صورة

### PayPal:
- `GET /api/config/paypal` - الحصول على PayPal Client ID

## 🎨 المميزات التصميمية

- ✅ تصميم متجاوب بالكامل (Responsive) لجميع الأجهزة
- ✅ واجهة مستخدم عصرية وجذابة
- ✅ رسوم متحركة سلسة (Animations)
- ✅ سلايدرات تفاعلية
- ✅ نظام ألوان متناسق
- ✅ تجربة مستخدم ممتازة (UX)

## 🔐 الأمان

- مصادقة JWT للمستخدمين
- تشفير كلمات المرور باستخدام Bcrypt
- حماية المسارات الخاصة
- التحقق من الصلاحيات (Admin/User)
- معالجة آمنة للملفات المرفوعة

## 📦 قاعدة البيانات

### النماذج الرئيسية:
- **User**: معلومات المستخدمين
- **Product**: المنتجات مع الصور والأسعار
- **Category**: فئات المنتجات
- **Order**: الطلبات مع معلومات الشحن والدفع

## 🧪 الاختبار

```bash
# تشغيل الاختبارات (إذا كانت متوفرة)
npm test
```

## 📝 ملاحظات التطوير

- المشروع يستخدم ES6 Modules
- Redux Toolkit للتحكم في الحالة
- RTK Query لطلبات API
- Tailwind CSS للتصميم
- Vite كأداة بناء سريعة

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع للميزة الجديدة (`git checkout -b feature/AmazingFeature`)
3. عمل Commit للتغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مرخص تحت MIT License.

## 👨‍💻 المطور

تم تطوير هذا المشروع بواسطة فريق التطوير.

## 📞 التواصل

للاستفسارات والدعم، يرجى فتح Issue في المستودع.

---

**ملاحظة**: تأكد من تحديث ملف `.env` بمعلوماتك الخاصة قبل التشغيل.

**تم التطوير بـ ❤️ للمهتمين بالحيوانات الأليفة**

